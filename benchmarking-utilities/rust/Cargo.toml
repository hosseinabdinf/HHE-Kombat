# for benchmark utils you need to add the following dependencies
# to your Cargo.toml file

[dependencies]
...
memory-stats = "1.2.0"
libc = "0.2"
jemallocator = "0.5"
jemalloc-ctl = "0.5"
crossterm = "0.29.0"
serde = "1.0.130"
bincode = "1.3.3"