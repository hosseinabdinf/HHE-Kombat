# Rust Benchmarking Utility

## How to Use

To utilize the benchmarking utility in your Rust project, first move the `utils` folder into your `src` directory. Then, use the provided sample `lib.rs` file to import the utility functions into your Rust code.