# C Benchmarking Utility

## How to Use

To utilize the benchmarking utility in your C/C++ project, first move the `utils` folder into your `src` directory. Then, use the following line in your project's CMakeFile.txt:

```
add_subdirectory(utils)
```